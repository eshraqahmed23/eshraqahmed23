# --- Business identity (used to personalize AI-drafted messages) ---
BUSINESS_NAME=Hilltop Realty
BUSINESS_TYPE=real estate agency
AGENT_NAME=Alex Rivera
AGENT_EMAIL=alex@hilltoprealty.com

# --- AI ---
# Get a key at https://platform.claude.com — required unless MOCK_AI=true
ANTHROPIC_API_KEY=sk-ant-...
# CLAUDE_MODEL=claude-opus-4-8
# MOCK_AI=true            # run the whole workflow without an API key (dev only)

# --- CRM (optional — omit to use the built-in local JSON CRM) ---
# HUBSPOT_ACCESS_TOKEN=pat-...

# --- Email via SendGrid (optional — omit for dry-run logging) ---
# SENDGRID_API_KEY=SG....
# FROM_EMAIL=alex@hilltoprealty.com

# --- SMS via Twilio (optional — omit for dry-run logging) ---
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_FROM_NUMBER=+15550001111

# PORT=3000
